# 复审报告 v3（2026-07-25，对 Codex a3f041f 的独立复审）

## 复审范围
- 全量基线测试：236 项全部通过（verifier/runs/2026-07-25-v3-baseline.md）
- 新增提交走读：policy/ 五个约束文件、model_router、workflow task engine、channel envelope、
  validate_governance.py、CI 现状
- 与深度研究报告（M1 治理基线）逐项对照

## 总体结论
Codex 这一轮的走向与研究报告结论一致：**从"堆功能"转向"治理制度化"**，并且落地质量高：

1. **policy/ 机器可读策略已成型**（safety/model-routing/task-engine/workflow/channel 五个约束文件），
   风险五级（read/change/privileged/irreversible/forbidden）与审批四模式
   （none/owner_exact/owner_elevated/owner_irreversible）定义完整，比报告示例更贴合本仓库实际；
2. **validate_governance.py 静态门禁**已进 CI，能拒绝"削弱红线/放开自主晋升/解绑载荷"等策略退化；
3. **M3/M4 地基提前打下**：工作流任务引擎、模型路由、统一渠道信封均有约束文件 + 技能 + 测试；
4. 我此前交付的五自功能（self_optimize/skill_forge/task_board/growth_daemon/growth_report）
   全部被保留并通过了 236 项测试，无冲突。

## 差距清单（本轮推进目标）
| # | 报告要求 | 现状 | 本轮对策 |
|---|---|---|---|
| 1 | 运行时统一策略引擎 | ❌ 只有静态校验脚本，运行时代码无统一 evaluate 入口 | 新增 app/core/policy.py（PolicyEngine） |
| 2 | privileged 双签审批 | ❌ permissions.py 仅单签 | owner_elevated→双签 180s、owner_irreversible→双签+二次确认 120s |
| 3 | 移动端确认规则 | ⚠️ channel 约束文件有原则，无运行时判定 | engine.evaluate(subject=) 落地 + 点名测试 |
| 4 | docs/SAFETY_POLICY.md、ROADMAP.md、intentions.template.json | ❌ 缺失 | 按报告框架落库并与 policy 事实对齐 |
| 5 | CI 供应链（security.yml/CodeQL/SBOM/密钥扫描/SHA 固定指引） | ⚠️ 仅轻量 ci.yml + governance 校验 | security.yml + 契约测试锁定门禁 |

## 走读确认无阻断问题的点
- policy/safety-constraints.v1.yaml 与 validate_governance.py 的必需字段自洽 ✅
- 审批三目录拆分（requests/decisions/consumed）与载荷指纹绑定仍完好 ✅
- 工作流任务引擎的证据门（evidence gates）与五自意向生命周期无语义冲突 ✅
- Docker 三服务 read_only/tmpfs/cap_drop/no-new-privileges 拓扑保持 ✅

## 风险与注意
- 策略引擎与既有 permissions/operations/validation 的接线必须**向后兼容**：
  engine 不可用时降级为现有行为，冲突时取更严格者（236 项既有测试为零回归硬约束）
- 第三方 GitHub Action 的真实 commit SHA 无法离线核验，CI 中采用"标签引用 + TODO 注释 + 文档如实声明"，
  不伪造 SHA
