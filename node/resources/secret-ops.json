{
  "id": "operations.secret-env.catalog",
  "name": "Owner-only managed environment secrets",
  "description": "List only masked secret-seat metadata through the governed Runner. Full reveal and new secret input are intentionally restricted to the local owner Secret Console and never enter Agent or model context.",
  "version": "1.0.0",
  "commands": [
    "make secret ARGS='targets'",
    "make secret ARGS='list <env-target>'",
    "make secret ARGS='reveal <env-target> <seat-id>'",
    "make secret ARGS='patch <env-target>'",
    "make secret ARGS='status <op-id>'"
  ],
  "operations": [
    {
      "capability": "server.secrets",
      "operation": "inventory_env",
      "risk": "read",
      "parameters": ["env_target"],
      "returns": "masked values, short fingerprints and inventory hash only"
    },
    {
      "capability": "server.secrets",
      "operation": "patch_env",
      "risk": "change",
      "owner_console_only": true,
      "returns": "per-seat actions and old/new short fingerprints only"
    }
  ],
  "security": {
    "agent_can_read_full_secret": false,
    "operation_request_contains_secret": false,
    "audit_contains_secret": false,
    "atomic_replace": true,
    "rollback_on_reload_failure": true
  }
}
