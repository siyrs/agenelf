# Default Agenelf topology.
# Import the audited production graph and the owner-authorized internal plaintext
# secret broker. Node Approval and private Prompt mounts are applied by the
# standard compose.override.yaml file.
include:
  - path: docker-compose.yml
  - path: compose.secret-chat.yaml
